export type ProjectType = {
  title: string;
  description: string;
  tags: string[];
  imageUrl: string;
  repoLink: string;
  siteLink: string;
};


export type SectionName = "Home" | "About" | "Projects" | "History" | "Skills" | "Contact";